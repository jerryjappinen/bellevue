// Use `offline-plugin` to let users cache your app
// NOTE: to enable plugin, set options in `main.js`, `config.build.js` and `config.dev.build.js`

// This file only defines the options: https://github.com/NekR/offline-plugin/blob/master/docs/options.md
module.exports = {

	// Plugin options
	// appShell: null,
	// caches: 'all',
	// publicPath: '',

	// responseStrategy: 'network-first',
	// updateStrategy: 'all',
	// externals: [],
	// excludes: [],
	// relativePaths: false,
	autoUpdate: true

}
